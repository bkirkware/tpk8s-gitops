apiVersion: spaces.tanzu.vmware.com/v1alpha1
kind: Space
metadata:
  name: kw-metal-dev
spec:
  template:
    spec:
      profiles:
      - name: apps
      resources:
        limits:
          cpu: "5"
          memory: 10Gi
  availabilityTargets:
  - name: kw-aws-dev
    replicas: 1
  updateStrategy:
    type: RollingUpdate
---
apiVersion: spaces.tanzu.vmware.com/v1alpha1
kind: SpaceDisruptionBudget
metadata:
  name: kw-metal-dev
spec:
  availabilityTargets:
  - name: kw-aws-dev
    minAvailable: 1
