apiVersion: spaces.tanzu.vmware.com/v1alpha1
kind: AvailabilityTarget
metadata:
  name: kirkware-vsphere-dev
  description: Availability Target for - vsphere - dev
  namespace: default
spec:
  affinity:
    clusterAffinity:
      clusterSelectorTerms:
      - matchExpressions:
        - key: kirkware-vsphere
          operator: Exists
        - key: kirkware-dev
          operator: Exists
