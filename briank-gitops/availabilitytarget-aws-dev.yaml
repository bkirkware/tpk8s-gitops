apiVersion: spaces.tanzu.vmware.com/v1alpha1
kind: AvailabilityTarget
metadata:
  name: kirkware-aws-dev
  description: Availability Target for - aws - dev
  namespace: default
spec:
  affinity:
    clusterAffinity:
      clusterSelectorTerms:
      - matchExpressions:
        - key: kirkware-aws
          operator: Exists
        - key: kirkware-dev
          operator: Exists
